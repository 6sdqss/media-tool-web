# Caddyfile — proxy toàn bộ traffic tới cổng nội bộ 8000, nơi Reflex --env
# prod phục vụ CẢ frontend lẫn backend chung 1 cổng (yêu cầu bắt buộc của
# Reflex ở prod mode — tách 2 cổng riêng sẽ báo lỗi lúc chạy).
:{$PORT}

reverse_proxy 127.0.0.1:8000
